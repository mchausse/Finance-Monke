import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FinanceMonke',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
